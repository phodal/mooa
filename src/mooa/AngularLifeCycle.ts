function bootstrap() {
  // load assets
}

function load() {
  // TBC
}

function mount() {
  // setContainer
}

function unmount() {
  // removeContainer
}

function unload() {
  // removeScript
}

const MooaLifeCycle = {
  bootstrap: bootstrap,
  load: load,
  mount: mount,
  unload: unload,
  unmount: unmount,
};

export default MooaLifeCycle;
